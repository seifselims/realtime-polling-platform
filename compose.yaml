  services:
    postgres:
      image: postgres:17
      environment:
        POSTGRES_USER: audience
        POSTGRES_PASSWORD: audience_dev
        POSTGRES_DB: audience
      ports:
        - "127.0.0.1:5432:5432"
      volumes:
        - postgres_data:/var/lib/postgresql/data

    redis:
      image: redis:7
      command: ["redis-server", "--appendonly", "yes"]
      ports:
        - "127.0.0.1:6379:6379"
      volumes:
        - redis_data:/data

  volumes:
    postgres_data:
    redis_data: